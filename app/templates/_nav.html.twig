<nav>
    <div>
        <a href="{{ path('app_manga_index') }}">Manga Manager</a>
    </div>
    <div>
        {% if app.user %}
            <a href="{{ path('app_logout') }}">{{ app.user.email }}</a>
        {% else %}
            <a href="{{ path('app_register') }}">Register</a>
            <a href="{{ path('app_login') }}">Login</a>
        {% endif %}
    </div>
</nav>